//your JS code here. If required.
// Declare two variables to store the input values
let firstInput, secondInput;

// Prompt the user to enter the first input and convert it to a number
firstInput = Number(prompt("Enter the first input"));

// Multiply the first input by 2 and store the result in a variable
let firstResult = firstInput * 2;

// Prompt the user to enter the second input and convert it to a number
secondInput = Number(prompt("Enter the second input"));

// Add the second input to the first result and store the final result in a variable
let finalResult = firstResult + secondInput;

// Display the final result using alert in the specified format
alert("The final result is: " + finalResult);
